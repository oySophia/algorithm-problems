#include <stdio.h>
#include <stdlib.h>

int main() {
	int n, i;
	while(scanf("%d", &n) != EOF) {
		if(n % 2) {
			printf("-1\n");
		} else {
			for(i = 1; i <= n; i += 2) {
				printf("%d %d ", i + 1, i);
			}
			printf("\n");
		}
	}
	return 0;
}
